# 📊 매수/매도 시점 확인 가이드

## 목차
1. [빠른 시작](#빠른-시작)
2. [상세 분석 보기](#상세-분석-보기)
3. [엑셀로 내보내기](#엑셀로-내보내기)
4. [제공되는 정보](#제공되는-정보)

---

## 🚀 빠른 시작

### 방법 1: 간단한 확인 (추천)

```bash
poetry run python check_my_trades.py
```

**출력 내용:**
- 📅 거래 타임라인 (날짜, 매수/매도, 가격)
- 💰 실현 손익
- 📊 거래 요약 통계
- 💾 엑셀 파일 자동 생성

**예시 출력:**
```
📋 매수/매도 시점 요약:

1. 2024-03-20 🔵 매도 @ ₩  74,373  사유: 진입
2. 2024-04-16 🔵 매도 @ ₩  77,723  사유: 중간선 돌파  ✅ +₩133,989
3. 2024-06-19 🔵 매도 @ ₩  78,889  사유: 진입
4. 2024-07-19 🔵 매도 @ ₩  82,363  사유: 중간선 돌파  ✅ +₩132,036

총 매수: 2회, 총 매도: 2회
```

### 방법 2: 상세 분석

```bash
poetry run python analyze_trades.py
```

**추가 정보:**
- 📈 각 거래 시점의 시장 상황 (시가/고가/저가/종가/거래량)
- 📊 볼린저 밴드 위치 (상단/중간/하단 밴드)
- 💼 포트폴리오 상태 변화
- 📉 일일 변동성
- 📅 요일별/시간대별 거래 통계

---

## 📋 제공되는 정보

### 1. 기본 거래 정보

각 거래마다 다음 정보를 확인할 수 있습니다:

```
거래 #1
--------------------------------------------------------------------------------
🔴 매수
종목:         005930 (삼성전자)
날짜:         2024-03-20 09:00:00
가격:         ₩74,373
수량:         40주
거래금액:     ₩2,974,918
진입 사유:    스퀴즈 돌파 (상단 밴드 돌파)
```

### 2. 매수/매도 사유

#### 매수 사유 (Entry Reason)
- **스퀴즈 돌파 (squeeze_breakout_buy)**
  - 스퀴즈 감지 후 가격이 상단 밴드를 돌파
  - 대역폭이 확장 중
  - 가장 강한 매수 신호

#### 매도 사유 (Exit Reason)
- **손절매 (stop_loss)**
  - 매수가 대비 5% 손실 (기본값)
  - 자동 손절 실행

- **중간선 하향 돌파 (middle_band_cross)**
  - 가격이 중간선 아래로 하락
  - 추세 전환 신호

- **상단 밴드 도달 (band_upper_exit)**
  - 가격이 상단 밴드에 도달 (익절)
  - 목표가 달성

### 3. 볼린저 밴드 정보

거래 시점의 볼린저 밴드 상태:

```
볼린저 밴드 (거래 시점):
  상단 밴드:  ₩78,699  ← 가격이 여기를 넘으면 매수
  중간선:     ₩74,774  ← 20일 이동평균선
  하단 밴드:  ₩70,849  ← 가격이 여기를 넘으면 매도(공매도)
  대역폭:     10.50%   ← 대역폭 (좁을수록 스퀴즈)
```

**대역폭 해석:**
- < 5%: 매우 좁음 (강한 스퀴즈)
- 5-10%: 좁음 (스퀴즈 가능)
- 10-15%: 보통
- > 15%: 넓음 (변동성 높음)

### 4. 시장 상황

거래 당일의 시장 데이터:

```
📈 시장 상황 (당일):
  시가:       ₩78,791
  고가:       ₩80,152  ← 당일 최고가
  저가:       ₩78,208  ← 당일 최저가
  종가:       ₩78,889  ← 당일 종가
  거래량:     24,168,863주
  일일 변동성: 2.46%   ← (고가-저가)/종가
```

### 5. 포트폴리오 변화

```
포트폴리오 상태:
  거래 전:    ₩13,131,753  ← 거래 전 총 자산
  거래 후:    ₩10,133,989  ← 거래 후 총 자산
  현금:       ₩7,136,225   ← 현금 잔고
```

### 6. 손익 정보 (매도 시)

```
✅ 실현 손익:  +₩132,036
보유 기간:    27일
수익률:       +4.48%
매수가:       ₩74,373
매도가:       ₩78,889
```

---

## 📊 거래 통계

### 종목별 통계

```
종목별 거래 횟수:
  005930: 매수 2회, 매도 2회
  035720: 매수 1회, 매도 1회
```

### 시간대별 통계

```
시간대별 거래:
  09:00 ~ 09:59: 3회  ← 장 시작 시간
  14:00 ~ 14:59: 1회
```

### 요일별 통계

```
요일별 거래:
  월요일: 1회
  수요일: 2회
  금요일: 1회
```

### 손익 분석

```
손익 거래 분석:
  수익 거래: 2회 (평균: ₩133,013)
  손실 거래: 0회
  총 실현손익: +₩266,026
```

---

## 💾 엑셀로 내보내기

### 자동 생성

`check_my_trades.py` 또는 `analyze_trades.py` 실행 시 자동으로 엑셀 파일 생성:

```
005930_거래내역_2024-01-01_2024-12-31.xlsx
```

### 엑셀 파일 내용

| 컬럼 | 설명 | 예시 |
|------|------|------|
| 날짜 | 거래 날짜 | 2024-03-20 |
| 시간 | 거래 시간 | 09:00:00 |
| 종목코드 | 6자리 코드 | 005930 |
| 매매구분 | 매수/매도 | 매수 |
| 가격 | 체결 가격 | 74,373 |
| 수량 | 거래 수량 | 40 |
| 거래금액 | 가격 × 수량 | 2,974,918 |
| 진입사유 | 매수 이유 | squeeze_breakout_buy |
| 청산사유 | 매도 이유 | stop_loss |
| 실현손익 | 손익 금액 | +133,989 |
| 상단밴드 | 볼린저 상단 | 78,699 |
| 중간선 | 이동평균선 | 74,774 |
| 하단밴드 | 볼린저 하단 | 70,849 |
| 대역폭 | 밴드 폭 | 0.105 |
| 포트폴리오(전) | 거래 전 자산 | 13,131,753 |
| 포트폴리오(후) | 거래 후 자산 | 10,133,989 |
| 현금잔고 | 현금 | 7,136,225 |

### 엑셀에서 활용

**필터링:**
- 수익 거래만 보기: "실현손익 > 0"
- 특정 종목만: "종목코드 = 005930"
- 특정 기간만: "날짜" 범위 설정

**피벗 테이블:**
- 종목별 평균 수익
- 월별 거래 횟수
- 요일별 수익률

**차트 생성:**
- 시간에 따른 포트폴리오 변화
- 누적 손익 그래프
- 거래 빈도 분석

---

## 🔍 상세 분석 예제

### Python 코드로 직접 분석

```python
from check_my_trades import quick_check_trades

# 거래 내역 확인
engine, report = quick_check_trades(
    stock_code="005930",
    start_date="2024-01-01",
    end_date="2024-12-31",
    seed_money=10_000_000
)

# 모든 거래 순회
for trade in engine.trades:
    print(f"{trade.execution_timestamp}: {trade.action.value}")
    print(f"  가격: ₩{trade.execution_price:,}")
    print(f"  사유: {trade.entry_reason or trade.exit_reason}")

    if trade.realized_pnl:
        print(f"  손익: ₩{trade.realized_pnl:,}")
```

### 특정 거래만 분석

```python
# 수익 거래만
profitable = [t for t in engine.trades if t.realized_pnl and t.realized_pnl > 0]
print(f"수익 거래: {len(profitable)}회")

# 손실 거래만
losses = [t for t in engine.trades if t.realized_pnl and t.realized_pnl < 0]
print(f"손실 거래: {len(losses)}회")

# 최대 수익 거래
if profitable:
    best_trade = max(profitable, key=lambda t: t.realized_pnl)
    print(f"최대 수익: ₩{best_trade.realized_pnl:,}")
    print(f"  날짜: {best_trade.execution_timestamp}")
```

### 보유 기간 분석

```python
# 매수-매도 쌍 찾기
buy_trades = [t for t in engine.trades if t.action.value == "BUY"]
sell_trades = [t for t in engine.trades if t.action.value == "SELL"]

for buy, sell in zip(buy_trades, sell_trades):
    holding_days = (sell.execution_timestamp - buy.execution_timestamp).days
    pnl_pct = ((sell.execution_price - buy.execution_price) / buy.execution_price) * 100

    print(f"{buy.stock_code}:")
    print(f"  매수: {buy.execution_timestamp.strftime('%Y-%m-%d')} @ ₩{buy.execution_price:,}")
    print(f"  매도: {sell.execution_timestamp.strftime('%Y-%m-%d')} @ ₩{sell.execution_price:,}")
    print(f"  보유: {holding_days}일")
    print(f"  수익률: {pnl_pct:+.2f}%")
```

---

## 🎯 실전 활용 팁

### 1. 거래 패턴 분석

**질문:**
- 어떤 요일에 거래가 많은가?
- 보통 며칠 보유하는가?
- 어떤 사유로 매수/매도가 많은가?

**확인 방법:**
```bash
poetry run python analyze_trades.py
# → 거래 요약 통계 섹션 확인
```

### 2. 최적 진입/청산 타이밍 찾기

**볼린저 밴드 위치 확인:**
- 가격이 상단 밴드 어느 정도 위에서 매수했는가?
- 중간선까지 떨어지기 전에 매도할 수 있었는가?

**확인 방법:**
- 엑셀 파일 열기
- "가격", "상단밴드", "중간선" 컬럼 비교

### 3. 손절매 분석

**질문:**
- 손절매가 적절히 작동했는가?
- 손절 후 가격이 회복했는가?

**확인 방법:**
```python
# 손절매 거래만 필터링
stop_loss_trades = [
    t for t in engine.trades
    if t.exit_reason == 'stop_loss'
]

for trade in stop_loss_trades:
    print(f"손절매: {trade.execution_timestamp}")
    print(f"  손실: ₩{trade.realized_pnl:,}")
```

### 4. 전략 개선

**데이터 기반 의사결정:**
1. 엑셀 파일 분석
2. 수익 거래의 공통점 찾기
3. 손실 거래의 패턴 찾기
4. 파라미터 조정

**예시:**
```
발견: 보유 기간이 30일 이상인 거래는 모두 손실
→ 해결: 최대 보유 기간 설정 추가
```

---

## 📚 관련 파일

| 파일 | 용도 |
|------|------|
| `check_my_trades.py` | 간단한 거래 확인 (추천) |
| `analyze_trades.py` | 상세 분석 |
| `backtest_with_real_data.py` | 실제 데이터로 백테스트 |
| `005930_거래내역_*.xlsx` | 엑셀 결과 파일 |

---

## 🆘 문제 해결

### 거래가 없음

**증상:**
```
❌ 거래 내역이 없습니다.
```

**해결:**
1. 기간을 더 길게 (1년 → 2년)
2. `squeeze_threshold_percent` 낮추기 (30 → 25 → 20)
3. 변동성이 큰 종목 선택

### 매수만 있고 매도가 없음

**원인:**
- 아직 청산 조건이 충족되지 않음
- 백테스트 기간이 너무 짧음

**해결:**
- 백테스트 종료일을 최신으로

### 엑셀 파일을 열 수 없음

**증상:**
```
openpyxl이 설치되지 않음
```

**해결:**
```bash
poetry add openpyxl
```

---

## 💡 빠른 참조

### 명령어

```bash
# 간단히 확인
poetry run python check_my_trades.py

# 상세 분석
poetry run python analyze_trades.py

# 실제 데이터로 백테스트
poetry run python backtest_with_real_data.py
```

### 거래 사유 코드

| 코드 | 한글 | 의미 |
|------|------|------|
| `squeeze_breakout_buy` | 스퀴즈 돌파 매수 | 가격이 상단 밴드 돌파 |
| `stop_loss` | 손절매 | 5% 손실 도달 |
| `middle_band_cross` | 중간선 돌파 | 중간선 하향 돌파 |
| `band_upper_exit` | 상단 밴드 익절 | 목표가 도달 |

---

**마지막 업데이트**: 2025-10-11
**관련 문서**: `사용법.md`, `데이터_가져오기_가이드.md`
